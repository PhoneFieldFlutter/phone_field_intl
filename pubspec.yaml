name: phone_field_intl
description: A customised Flutter TextFormField to input international phone number along with country code.
version: 4.0.0
homepage: https://github.com/PhoneFieldFlutter/phone_field_intl

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  test: ^1.17.12

flutter:
  assets:
    - assets/flags/
